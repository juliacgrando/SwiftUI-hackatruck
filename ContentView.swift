
import SwiftUI

/*struct ContentView: View {
    var body: some View {
 
        HStack{
            Rectangle()
                .frame(width:100,height: 100)
                .foregroundColor(/*@START_MENU_TOKEN@*/.blue/*@END_MENU_TOKEN@*/)
                .padding()
            Spacer()
            Rectangle()
                .frame(width:100, height: 100)
                .foregroundColor(.red)
                .padding()
        }
 
            Spacer()
 
        HStack{
            Rectangle()
                .frame(width:100,height: 100)
                .foregroundColor(.purple)
                .padding()
            Spacer()
            Rectangle()
                .frame(width:100,height: 100)
                .foregroundColor(.green)
                .padding()
            }
        }
    }

struct ContentView: View{
    var body: some View{
        VStack{
            HStack{
                Image("gatomeme")
                    .resizable()
                    .scaledToFit()
                    .clipShape(Circle())
                    .padding()
                VStack(spacing:10){
                    Text("ALOUUUUUUUU")
                        .foregroundStyle(.red)
                    Text(" Galera de peao")
                        .foregroundStyle(.blue)
                    Text("quem ta feliz bate forte com a máo")
                        .foregroundStyle(.green)
                }
                .padding()
                }
            }
        }
    }*/

    #Preview {
    ContentView()
}
